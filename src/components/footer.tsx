const Footer = () => {
    return (
        <footer className="bg-gray-100 text-gray-600 text-center py-4">
            <p>© {new Date().getFullYear()} ShopLogo. Tous droits réservés.</p>
        </footer>
    );
};

export default Footer;